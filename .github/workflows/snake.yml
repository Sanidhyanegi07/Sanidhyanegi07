name: Generate Snake & Calendar

on:
  schedule:
    - cron: '0 0 * * *'
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Generate contribution calendar
        run: |
          mkdir -p dist
          python .github/scripts/generate-calendar.py ${{ github.repository_owner }} dist/contribution-calendar.svg
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate contribution snake
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg?color_snake=#58a6ff&color_dots=#161b22,#0d2240,#1158a0,#1f6feb,#58a6ff
            dist/github-snake-dark.svg?palette=github-dark&color_snake=#58a6ff&color_dots=#161b22,#0d2240,#1158a0,#1f6feb,#58a6ff

      - name: Decorate snake with dates & HUD telemetry
        run: |
          python .github/scripts/decorate-snake.py dist/github-snake-dark.svg dist/github-snake.svg

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
